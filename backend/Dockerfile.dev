FROM node:22-alpine
WORKDIR /app

# Install deps separately for better layer caching
COPY package*.json ./
RUN npm ci

# Mount source at runtime via volume — no COPY needed for dev
EXPOSE 3001
CMD ["npm", "run", "dev"]
